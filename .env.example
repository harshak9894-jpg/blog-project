PORT=5000
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/blogDB
JWT_SECRET=replace-with-a-long-random-secret
NODE_ENV=production
# Optional for Render persistent disk, example: /var/data/uploads
UPLOADS_DIR=
