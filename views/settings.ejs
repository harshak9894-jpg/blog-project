<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Settings</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/style.css" rel="stylesheet">
</head>
<body class="app-body <%= user ? 'sidebar-expanded' : '' %>">

<% if (user) { %>
  <%- include("partials/sidebar") %>
<% } else { %>
  <%- include("partials/navbar") %>
<% } %>

<main class="<%= user ? 'page-wrap page-wrap-with-sidebar' : 'page-wrap' %>">
  <% if (success.length > 0) { %>
    <div class="alert alert-success"><%= success %></div>
  <% } %>

  <% if (error.length > 0) { %>
    <div class="alert alert-danger"><%= error %></div>
  <% } %>

  <section class="content-card settings-more-card">
    <div class="settings-more-actions d-flex flex-column gap-2">
      <a href="/saved" class="btn btn-soft text-decoration-none text-start d-flex align-items-center justify-content-between">
        <span>Saved posts</span>
        <span class="meta-text" aria-hidden="true">&#128278;</span>
      </a>
      <a href="/logout" class="btn btn-outline-danger text-decoration-none text-start">Log out</a>
    </div>
  </section>
</main>

</body>
</html>
